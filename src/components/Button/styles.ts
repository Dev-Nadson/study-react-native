import { StyleSheet } from "react-native";

const button_styles = StyleSheet.create({
    container: {
        backgroundColor: "#2c4cb1",
        height: 48,
        width: "100%",
        borderRadius: 8,
        alignItems: "center",
        justifyContent: "center"
    },
    title: {
        color: "#FFF",
        fontSize: 14,
        fontWeight: 600
    }
})

export { button_styles }